warning: in the working copy of 'pyproject.toml', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'uv.lock', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/.env.example b/.env.example[m
[1mindex d8769ea..a698de6 100644[m
[1m--- a/.env.example[m
[1m+++ b/.env.example[m
[36m@@ -1,2 +1,3 @@[m
 ENV=local[m
[31m-GOOGLE_API_KEY=[m
\ No newline at end of file[m
[32m+[m[32mGOOGLE_API_KEY=[m
[32m+[m[32mTYPHOON_API_KEY=[m
[1mdiff --git a/pyproject.toml b/pyproject.toml[m
[1mindex 6d2d05b..7fbae8a 100644[m
[1m--- a/pyproject.toml[m
[1m+++ b/pyproject.toml[m
[36m@@ -15,7 +15,9 @@[m [mdependencies = [[m
     "langchain-community>=0.4.2",[m
     "langchain-google-genai>=4.3.3",[m
     "langchain-text-splitters>=1.1.2",[m
[32m+[m[32m    "pillow>=12.3.0",[m
     "pypdf>=6.16.0",[m
[32m+[m[32m    "pypdfium2>=5.13.0",[m
     "python-dotenv>=1.2.2",[m
     "sse-starlette>=3.4.8",[m
     "uvicorn>=0.52.3",[m
[36m@@ -43,4 +45,4 @@[m [mdev = [[m
 line-length = 100[m
 [m
 [tool.ruff.lint][m
[31m-select = ["E", "F", "I", "UP", "B"][m
\ No newline at end of file[m
[32m+[m[32mselect = ["E", "F", "I", "UP", "B"][m
[1mdiff --git a/src/rag_gis_api/__init__.py b/src/rag_gis_api/__init__.py[m
[1mindex 5d0798b..b58470e 100644[m
[1m--- a/src/rag_gis_api/__init__.py[m
[1m+++ b/src/rag_gis_api/__init__.py[m
[36m@@ -13,9 +13,14 @@[m [mENV = os.getenv("ENV", "production")[m
 [m
 GOOGLE_API_KEY = os.getenv("GOOGLE_API_KEY")[m
 [m
[32m+[m[32mTYPHOON_API_KEY = os.getenv("TYPHOON_API_KEY")[m
[32m+[m
 if not GOOGLE_API_KEY:[m
     raise RuntimeError(f"GOOGLE_API_KEY not set. Add it to {PROJECT_ROOT / '.env'}")[m
 [m
[32m+[m[32mif not TYPHOON_API_KEY:[m
[32m+[m[32m    raise RuntimeError(f"TYPHOON_API_KEY not set. Add it to {PROJECT_ROOT / '.env'}")[m
[32m+[m
 [m
 def main() -> None:[m
     print("Hello from rag-gis-api!")[m
[1mdiff --git a/src/rag_gis_api/services/ingest/load_pdf.py b/src/rag_gis_api/services/ingest/load_pdf.py[m
[1mindex a0d970c..005477d 100644[m
[1m--- a/src/rag_gis_api/services/ingest/load_pdf.py[m
[1m+++ b/src/rag_gis_api/services/ingest/load_pdf.py[m
[36m@@ -16,39 +16,84 @@[m [mMIN_READABLE_CHARS = 30[m
 # there but unreadable.[m
 CID_PATTERN = re.compile(r"\(cid:\d+\)")[m
 [m
[31m-# Share of the visible characters allowed to be control bytes or replacement[m
[31m-# characters. Pages PyPDF decoded with the wrong font encoding sit well above[m
[31m-# this (0.11 and up in documents/), readable pages sit at 0.00.[m
[31m-MAX_JUNK_RATIO = 0.02[m
 [m
[31m-# The corpus is Thai law, so a page with next to no Thai letter came out of the[m
[31m-# wrong code page, e.g. Thai read as Latin-1 ("Àπâ“ 39 √“™°‘®®“πÿ") or left as[m
[31m-# glyph names ("/afii59723;"). Mis-decoded pages sit at 0.01 and below, while[m
[31m-# the most Latin-heavy readable pages, the tables of species names, still reach[m
[31m-# 0.48.[m
[31m-MIN_THAI_LETTER_RATIO = 0.15[m
[31m-[m
[31m-# Below this a page is a table of numbers or a form, not prose, so the Thai[m
[31m-# check has too little to go on and is skipped.[m
[31m-MIN_LETTERS_FOR_THAI_CHECK = 20[m
[32m+[m[32mCOMMON_THAI_WORDS = ([m
[32m+[m[32m    "ที่",[m
[32m+[m[32m    "การ",[m
[32m+[m[32m    "และ",[m
[32m+[m[32m    "ของ",[m
[32m+[m[32m    "ให้",[m
[32m+[m[32m    "ใน",[m
[32m+[m[32m    "เป็น",[m
[32m+[m[32m    "พ.ศ",[m
[32m+[m[32m    "ประกาศ",[m
[32m+[m[32m    "มาตรา",[m
[32m+[m[32m    "ข้อ",[m
[32m+[m[32m    "ความ",[m
[32m+[m[32m    "หรือ",[m
[32m+[m[32m)[m
[32m+[m[32m# ฟอนต์ไทยรุ่นเก่า (Angsana, Cordia, Sarabun ฯลฯ) วาดสระและวรรณยุกต์ที่ต้องเลื่อน[m
[32m+[m[32m# ตำแหน่งเป็น glyph ใน Private Use Area ตามผังกลางของ Microsoft แต่ ToUnicode ไม่[m
[32m+[m[32m# map กลับ ทุกค่าในตารางนี้ decode จากบริบทจริงในคลัง[m
[32m+[m[32mPUA_MAP = str.maketrans([m
[32m+[m[32m    {[m
[32m+[m[32m        "\uf701": "\u0e34",  # ิ เช่น ปิโตรเคมี[m
[32m+[m[32m        "\uf702": "\u0e35",  # ี เช่น ทั้งปี[m
[32m+[m[32m        "\uf703": "\u0e36",  # ึ เช่น ฝึกอบรม[m
[32m+[m[32m        "\uf704": "\u0e37",  # ื เช่น ฟื้นฟู[m
[32m+[m[32m        "\uf710": "\u0e31",  # ั เช่น ฝังกลบ[m
[32m+[m[32m        "\uf712": "\u0e47",  # ็ เช่น เป็น[m
[32m+[m[32m        "\uf705": "\u0e48",  # ่ เช่น ผู้ป่วย[m
[32m+[m[32m        "\uf70a": "\u0e48",  # ่ เช่น เล่ม[m
[32m+[m[32m        "\uf713": "\u0e48",  # ่ เช่น ชายฝั่ง[m
[32m+[m[32m        "\uf706": "\u0e49",  # ้ เช่น ไฟฟ้า[m
[32m+[m[32m        "\uf70b": "\u0e49",  # ้ เช่น หน้า[m
[32m+[m[32m        "\uf714": "\u0e49",  # ้ เช่น ปั้นดินเผา[m
[32m+[m[32m        "\uf707": "\u0e4a",  # ๊ เช่น โป๊ะ[m
[32m+[m[32m        "\uf70c": "\u0e4a",  # ๊ เช่น ก๊าซ[m
[32m+[m[32m        "\uf708": "\u0e4b",  # ๋ เช่น ปุ๋ย[m
[32m+[m[32m        "\uf709": "\u0e4c",  # ์ เช่น แสตมป์[m
[32m+[m[32m        "\uf70e": "\u0e4c",  # ์ เช่น หลักเกณฑ์[m
[32m+[m[32m    }[m
[32m+[m[32m)[m
[32m+[m
[32m+[m[32mMIN_COMMON_WORDS = 2[m
[32m+[m
[32m+[m[32mCONSONANT = "\u0e01-\u0e2e"  # ก - ฮ[m
[32m+[m[32mMARK = "\u0e31\u0e34-\u0e3a\u0e47-\u0e4e"  # สระและวรรณยุกต์[m
[32m+[m[32mTONE = "\u0e48-\u0e4b"  # วรรณยุกต์[m
[32m+[m
[32m+[m[32m# ช่องว่างที่คั่นพยัญชนะกับสระบน/ล่าง ไม่ใช่ช่องว่างจริง เกิดจากการจัดวาง glyph[m
[32m+[m[32mSPLIT_MARK = re.compile(f"(?<=[{CONSONANT}{MARK}])[ \t]+(?=[{MARK}])")[m
[32m+[m
[32m+[m[32m# นิคหิตกับสระอาที่ควรรวมเป็น ำ อาจมีวรรณยุกต์หรือช่องว่างคั่น[m
[32m+[m[32mSPLIT_SARA_AM = re.compile(f"\u0e4d[ \t]*([{TONE}]?)[ \t]*\u0e32")[m
[32m+[m
[32m+[m[32m# นิคหิตที่หายไปเป็นช่องว่าง[m
[32m+[m[32mLOST_NIKHAHIT = re.compile(f"([{CONSONANT}][{TONE}]?) [\u0e32\u0e33]")[m
[32m+[m
[32m+[m
[32m+[m[32mdef normalize_thai(text: str) -> str:[m
[32m+[m[32m    """คืนข้อความที่ประกอบ ำ กลับ และแทน glyph PUA ด้วยอักขระไทยจริง"""[m
[32m+[m[32m    text = text.translate(PUA_MAP)[m
[32m+[m[32m    text = SPLIT_MARK.sub("", text)[m
[32m+[m[32m    text = SPLIT_SARA_AM.sub(lambda m: m.group(1) + "\u0e33", text)[m
[32m+[m
[32m+[m[32m    return LOST_NIKHAHIT.sub(lambda m: m.group(1) + "\u0e33", text)[m
 [m
 [m
 def is_junk(character: str) -> bool:[m
     """Tell whether a character is a decoding failure rather than text."""[m
[31m-    return character == "�" or unicodedata.category(character) == "Cc"[m
[31m-[m
[31m-[m
[31m-def is_thai(character: str) -> bool:[m
[31m-    return "฀" <= character <= "๿"[m
[32m+[m[32m    return character == "�" or unicodedata.category(character) in ("Cc", "Co")[m
 [m
 [m
 def is_readable(text: str) -> bool:[m
     """[m
     Tell whether PyPDF made sense of a page.[m
 [m
[31m-    A page fails when it holds (almost) no text, when its glyphs came back as[m
[31m-    "(cid:NN)" placeholders, when too much of it is control bytes, or when its[m
[31m-    letters are not Thai. Those are the pages OCR has to redo.[m
[32m+[m[32m    A page has to hold enough text, decode without leftover control bytes or[m
[32m+[m[32m    unmapped glyphs, and read as Thai prose. Anything else goes to OCR: losing[m
[32m+[m[32m    a good page there costs less than letting a garbled one reach the store.[m
     """[m
     stripped = text.strip()[m
 [m
[36m@@ -58,32 +103,19 @@[m [mdef is_readable(text: str) -> bool:[m
     if CID_PATTERN.search(stripped):[m
         return False[m
 [m
[31m-    visible = [character for character in stripped if not character.isspace()][m
[31m-[m
[31m-    if not visible:[m
[32m+[m[32m    if any(is_junk(character) for character in stripped if not character.isspace()):[m
         return False[m
 [m
[31m-    junk = sum(1 for character in visible if is_junk(character))[m
[31m-[m
[31m-    if junk / len(visible) > MAX_JUNK_RATIO:[m
[31m-        return False[m
[31m-[m
[31m-    letters = [character for character in visible if character.isalpha()][m
[31m-[m
[31m-    if len(letters) < MIN_LETTERS_FOR_THAI_CHECK:[m
[31m-        return True[m
[31m-[m
[31m-    thai = sum(1 for character in letters if is_thai(character))[m
[31m-[m
[31m-    return thai / len(letters) >= MIN_THAI_LETTER_RATIO[m
[32m+[m[32m    found = sum(1 for word in COMMON_THAI_WORDS if word in stripped)[m
[32m+[m[32m    return found >= MIN_COMMON_WORDS[m
 [m
 [m
 async def read_pages(path: Path, ocr_queue: asyncio.Queue[Document]) -> list[Document]:[m
     """[m
[31m-    Read a PDF page by page and return the pages PyPDF made sense of.[m
[32m+[m[32m    Read a PDF page by page and return every page in order.[m
 [m
[31m-    Every other page goes on `ocr_queue`, so OCR can pick it up while the next[m
[31m-    pages are still being read.[m
[32m+[m[32m    Pages PyPDF could not read also go on `ocr_queue`, so OCR can fill them in[m
[32m+[m[32m    while the next pages are still being read.[m
     """[m
     loader = PyPDFLoader(str(path))[m
     pages = loader.lazy_load()[m
[36m@@ -97,9 +129,9 @@[m [masync def read_pages(path: Path, ocr_queue: asyncio.Queue[Document]) -> list[Doc[m
         if page is None:[m
             break[m
 [m
[31m-        if is_readable(page.page_content):[m
[31m-            documents.append(page)[m
[31m-        else:[m
[32m+[m[32m        page.page_content = normalize_thai(page.page_content)[m
[32m+[m[32m        documents.append(page)[m
[32m+[m[32m        if not is_readable(page.page_content):[m
             await ocr_queue.put(page)[m
 [m
     return documents[m
[36m@@ -118,7 +150,7 @@[m [masync def read_pages_with_ocr(path: Path) -> list[Document]:[m
     finally:[m
         ocr_task.cancel()[m
 [m
[31m-    return documents[m
[32m+[m[32m    return [page for page in documents if page.page_content][m
 [m
 [m
 def load_pdf(path: Path) -> list[Document]:[m
[1mdiff --git a/src/rag_gis_api/services/ingest/ocr.py b/src/rag_gis_api/services/ingest/ocr.py[m
[1mindex d2e9bb2..d79d557 100644[m
[1m--- a/src/rag_gis_api/services/ingest/ocr.py[m
[1m+++ b/src/rag_gis_api/services/ingest/ocr.py[m
[36m@@ -1,20 +1,107 @@[m
 import asyncio[m
[32m+[m[32mimport io[m
[32m+[m[32mimport json[m
[32m+[m[32mfrom pathlib import Path[m
 [m
[32m+[m[32mimport pypdfium2[m
[32m+[m[32mimport requests[m
 from langchain_core.documents import Document[m
 [m
[32m+[m[32mfrom rag_gis_api import TYPHOON_API_KEY[m
 [m
[31m-async def consume_ocr_queue(ocr_queue: asyncio.Queue[Document]) -> None:[m
[31m-    """[m
[31m-    Drain the pages PyPDF could not read.[m
[32m+[m[32mMODEL = "typhoon-ocr"[m
[32m+[m
[32m+[m[32m# Plain text back, rather than a structured layout the caller would have to parse.[m
[32m+[m[32mTASK_TYPE = "default"[m
[32m+[m
[32m+[m[32mMAX_TOKEN = 8192[m
[32m+[m
[32m+[m[32m# Only the likeliest tokens adding up to this share stay in play[m
[32m+[m[32mTOP_P = 0.6[m
[32m+[m
[32m+[m[32m# OCR wants the same page to read the same way every time, so sampling stays[m
[32m+[m[32m# close to deterministic.[m
[32m+[m[32mTEMPERATURE = 0.1[m
[32m+[m
[32m+[m[32m# A vision model that loses its place tends to repeat a line forever[m
[32m+[m[32mREPETITION_PENALTY = 1.2[m
[32m+[m
[32m+[m[32m# Typhoon's OCR endpoint and the model that serves it.[m
[32m+[m[32mURL = "https://api.opentyphoon.ai/v1/ocr"[m
[32m+[m
[32m+[m[32m# The scans in this corpus sit at 300 DPI, so rendering a little above that[m
[32m+[m[32m# keeps the tone marks crisp without inventing detail the original never held.[m
[32m+[m[32mRENDER_DPI = 400[m
[32m+[m
[32m+[m
[32m+[m[32mdef render_page(path: Path, page_number: int) -> bytes:[m
[32m+[m[32m    """Render the page PyPDF could not read as a PNG for OCR to pick up."""[m
[32m+[m[32m    document = pypdfium2.PdfDocument(path)[m
[32m+[m
[32m+[m[32m    try:[m
[32m+[m[32m        image = document[page_number].render(scale=RENDER_DPI / 72).to_pil()[m
[32m+[m[32m    finally:[m
[32m+[m[32m        document.close()[m
[32m+[m
[32m+[m[32m    buffer = io.BytesIO()[m
[32m+[m[32m    image.save(buffer, format="PNG", optimize=True)[m
[32m+[m
[32m+[m[32m    return buffer.getvalue()[m
 [m
[31m-    OCR is not wired up yet, so a page taken off the queue is only reported and[m
[31m-    then dropped: its text never reaches the vector store. Runs until the task[m
[31m-    is cancelled.[m
[31m-    """[m
[32m+[m
[32m+[m[32mdef extract_text_from_image([m
[32m+[m[32m    page,[m
[32m+[m[32m):[m
[32m+[m[32m    image = render_page(page.metadata["source"], page.metadata["page"])[m
[32m+[m[32m    files = {"file": ("page.png", image, "image/png")}[m
[32m+[m
[32m+[m[32m    data = {[m
[32m+[m[32m        "model": MODEL,[m
[32m+[m[32m        "task_type": TASK_TYPE,[m
[32m+[m[32m        "max_tokens": str(MAX_TOKEN),[m
[32m+[m[32m        "temperature": str(TEMPERATURE),[m
[32m+[m[32m        "top_p": str(TOP_P),[m
[32m+[m[32m        "repetition_penalty": str(REPETITION_PENALTY),[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32m    headers = {"Authorization": f"Bearer {TYPHOON_API_KEY}"}[m
[32m+[m
[32m+[m[32m    response = requests.post(URL, files=files, data=data, headers=headers)[m
[32m+[m
[32m+[m[32m    if response.status_code == 200:[m
[32m+[m[32m        result = response.json()[m
[32m+[m
[32m+[m[32m        # Extract text from successful results[m
[32m+[m[32m        extracted_texts = [][m
[32m+[m[32m        for page_result in result.get("results", []):[m
[32m+[m[32m            if page_result.get("success") and page_result.get("message"):[m
[32m+[m[32m                content = page_result["message"]["choices"][0]["message"]["content"][m
[32m+[m[32m                try:[m
[32m+[m[32m                    # Try to parse as JSON if it's structured output[m
[32m+[m[32m                    parsed_content = json.loads(content)[m
[32m+[m[32m                    text = parsed_content.get("natural_text", content)[m
[32m+[m[32m                except json.JSONDecodeError:[m
[32m+[m[32m                    text = content[m
[32m+[m[32m                extracted_texts.append(text)[m
[32m+[m[32m            elif not page_result.get("success"):[m
[32m+[m[32m                filename = page_result.get("filename", "unknown")[m
[32m+[m[32m                error = page_result.get("error", "Unknown error")[m
[32m+[m
[32m+[m[32m                print(f"Error processing {filename}: {error}")[m
[32m+[m
[32m+[m[32m        return "\n".join(extracted_texts)[m
[32m+[m[32m    else:[m
[32m+[m[32m        print(f"Error: {response.status_code}")[m
[32m+[m[32m        print(response.text)[m
[32m+[m[32m        return None[m
[32m+[m
[32m+[m
[32m+[m[32masync def consume_ocr_queue(ocr_queue: asyncio.Queue[Document]) -> None:[m
[32m+[m[32m    """Drain the pages PyPDF could not read."""[m
     while True:[m
         page = await ocr_queue.get()[m
[31m-[m
         try:[m
[31m-            print(f"OCR  {page.metadata.get('source')} page {page.metadata.get('page')} (dropped)")[m
[32m+[m[32m            page.page_cont